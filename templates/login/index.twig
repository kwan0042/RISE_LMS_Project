{# Login index page #}

{% extends 'login/common.twig' %}

{% block content %}
    {{ parent() }}
    <h2>Log in as ...</h2>
    <!-- Role Selection -->
    <div class="role-selection-buttons col-10 col-sm-5 col-md-4 col-lg-3">
        <a href="{{ url_for('login.student') }}" class="button button1">Student</a>
        <a href="{{ url_for('login.facilitator') }}" class="button button2">Facilitator</a>
        <a href="{{ url_for('login.administrator') }}" class="button button3">Administrator</a>
    </div>
{% endblock %}